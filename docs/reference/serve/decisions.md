# Decisions log

Intentional deviations from `vercel/serve` behavior (`adapted` status) and explicit non-goals (`rejected` status). Each entry must reference the affected requirement ID(s) and explain the rationale.

## Format

```
## D-<NNN>: <short title>

Date: YYYY-MM-DD
Affected requirements: SRV-<AREA>-<NNN>, ...
Status: adapted | rejected
Reason: <why we deviate>
Impact: <what users / oracle tests should expect>
```

## Entries

## D-001: No Node.js middleware API

Date: 2026-05-07
Affected requirements: (none — entire `serve-handler` API surface)
Status: rejected
Reason: IrServe is a CLI binary written in Rust. The Node-specific `handler(request, response, config, methods)` middleware API has no analog in a Rust HTTP server, and exposing an embeddable library is outside the MVP scope per anti-hallucination rule #5.
Impact: IrServe MUST NOT be used as a drop-in replacement for `serve-handler` in Node code paths. The configuration *file format* (`serve.json`) is in scope; the *programmatic API* is not.

## D-002: No exact terminal output / stdout formatting

Date: 2026-05-07 (amended 2026-05-10 for the SRV-CLI-016 scope clarification)
Affected requirements: SRV-CLI-014 (`-d`/`--debug`), SRV-CLI-015 (`-L`/`--no-request-logging`), SRV-CLI-016 (port-switching warning text only — the bind/exit behavior IS contractual; only the wording is implementation-defined per D-002), SRV-CLI-019 (`--help`/`--version`)
Status: rejected
Reason: The `serve` CLI uses `chalk`, `boxen`, and a specific log format (date prefix, IP, status, ms-elapsed). Mirroring this byte-for-byte adds churn without functional value and is excluded by anti-hallucination rule #5.
Impact: Oracle tests MUST NOT compare stdout/stderr text. Verbosity flags (`-d`, `-L`) are accepted; their effect on logging is implementation-defined. Per the Stage-6h amendment: SRV-CLI-016's bind/exit semantics (default retry on `EADDRINUSE` vs. non-zero exit under `--no-port-switching`) are contractual per D-016; only the stderr message wording falls under D-002.

## D-003: No exact HTML/CSS markup of the directory listing or error pages

Date: 2026-05-07
Affected requirements: SRV-DLST-001, SRV-FILE-002 (HTML branch), SRV-FILE-003 (HTML branch)
Status: rejected
Reason: The directory listing and the default error page are styled HTML templates inside `serve-handler`. Their visual design is not a contractual interface; only the *existence*, status code, and `Content-Type` of the response are. (See anti-hallucination rule #5.)
Impact: Oracle tests MUST NOT compare HTML body bytes for listings or for the no-`<status>.html` error path. The JSON branches of both (which carry structured data) ARE in scope.

## D-004: No bug-for-bug parity with `serve`

Date: 2026-05-07
Affected requirements: (all)
Status: rejected
Reason: Anti-hallucination rule #5. Where `serve` exhibits a behavior that looks unintentional (e.g. the JSON listing leaking absolute filesystem paths in its `dir` field — see Q-008; or the multi-slash-collapse only firing when `trailingSlash` is set — see Q-006), IrServe is free to diverge with a documented adaptation.
Impact: Future SRVs that catch a quirk should record an `adapted` status here when IrServe chooses not to mirror it. This is a meta-decision; it does not by itself rule out matching `serve`.

## D-005: Clipboard side effect is not implemented

Date: 2026-05-07
Affected requirements: SRV-CLI-011 (`-n`/`--no-clipboard`)
Status: rejected
Reason: Writing the bound URL to the system clipboard on startup is environment-coupled (display server, OS-specific clipboard daemons), interferes with non-interactive use, and is not part of the HTTP contract.
Impact: IrServe MUST accept the `-n`/`--no-clipboard` flag without error so existing scripts keep working, but it has no observable effect because IrServe never touches the clipboard.

## D-006: HTTP compression is L3-priority, not MVP-mandatory

Date: 2026-05-07
Affected requirements: SRV-CLI-012 (`-u`/`--no-compression`)
Status: adapted
Reason: `serve` uses the `compression` connect/express middleware with default settings. Implementing equivalent behavior in Rust adds dependencies (`flate2`, content-type sniffing, threshold logic) that are not justified for an L0–L2 MVP. The `--no-compression` flag stays in the CLI.
Impact: Until L3 work begins, IrServe MAY ignore `--no-compression` and serve responses uncompressed regardless. The `Vary: Accept-Encoding` header MAY still be omitted in this early phase; oracle tests for compression are deferred until the implementation lands.

## D-007: Sanitized JSON directory listing

Date: 2026-05-07
Affected requirements: SRV-DLST-001
Status: adapted
Reason: `serve`'s JSON directory listing exposes a `dir` field that contains the absolute filesystem path of the listed directory on the host machine (verified by probe `listing-unlisted`; tracked as Q-008). That value leaks deployment topology — username, deployment root, container layout — to any client that requests `Accept: application/json` against a directory URL. The README does not specify the field, so this is an implementation accident upstream rather than a contractual surface; `D-004` (no bug-for-bug parity) authorizes the divergence.
Impact: IrServe SHALL preserve the JSON-listing content-negotiation behavior (`Accept: application/json` returns `application/json; charset=utf-8` with the `{"files":[...], "directory":..., "paths":...}` shape) but the `dir` field — and any other field carrying a host-absolute path — MUST be rendered relative to the served root (e.g. `"."` for the root itself, `"sub"` for a `sub/` subdirectory). Closes Q-008.

## D-008: First-slice strict-L0 cutoff

Date: 2026-05-08
Affected requirements: SRV-CFG-001, SRV-CFG-002, SRV-FILE-003, SRV-ROUT-001, SRV-ROUT-002, SRV-ROUT-003, SRV-ROUT-004, SRV-ROUT-005, SRV-ROUT-006, SRV-RDIR-001, SRV-RDIR-002, SRV-RDIR-003, SRV-RWRT-001, SRV-DLST-001, SRV-DLST-002, SRV-DLST-003, SRV-CLI-003, SRV-CLI-006, SRV-CLI-008, SRV-CLI-009, SRV-CLI-010, SRV-CLI-011, SRV-CLI-014, SRV-CLI-015, SRV-CLI-016 (deferred from the first IrServe release; not removed from the contract)
Status: adapted
Reason: The first implementation proposal (`openspec/changes/001-port-minimal-static-server`, Stage 5a) deliberately scopes the initial Rust port to strict L0 — exactly eight SRVs (SRV-CLI-001, SRV-CLI-002, SRV-CLI-007, SRV-CLI-019, SRV-FILE-001, SRV-FILE-002, SRV-FILE-004, SRV-FILE-005). The slice exists to prove the methodology end-to-end against the existing oracle harness on the smallest possible code surface, not to ship a useful product. This is a release-scoping decision, not a behavioral divergence from `serve`.
Impact: The first IrServe release responds to L0 inputs only. L1+ inputs (e.g. `-l tcp://...`, `serve.json`-driven `cleanUrls`, configured redirects, directory listings) yield strict CLI rejection or absent-feature behavior, not parity with `serve`. No SRV bodies change. Subsequent changes (`002-…` onward) are sequenced to deliver L1 and L2 behavior; the deferred SRVs above stay `verified`/`accepted` in the contract throughout. Per-SRV un-deferral is recorded in subsequent D-NNN entries (D-009 un-defers SRV-CFG-001 / SRV-CFG-002 in Stage 6a).

## D-009: Stage 6a un-defers `serve.json` loading

Date: 2026-05-09
Affected requirements: SRV-CFG-001, SRV-CFG-002, SRV-CLI-009 (un-deferred from D-008's list)
Status: adapted
Reason: Change `003-load-serve-json` (Stage 6a) lands the configuration loader, the `-c/--config` CLI flag (closing SRV-CLI-009), and the `public` field surface. SRV-CFG-001's oracle list grows by ORC-064 (public field), ORC-065 (missing explicit), ORC-066 (malformed JSON), ORC-067 (explicit overrides default with observable `public` effect). SRV-CLI-009 was already `verified` via ORC-006; this change un-defers it from D-008 because the flag is now actually wired in IrServe. Stage 6h's row in `docs/stage6_l1_l2_capabilities.md` is updated to drop SRV-CLI-009 from its scope. The remaining schema-map fields (cleanUrls, redirects, rewrites, headers, etc.) are parsed into the typed configuration but their per-area behavior is delivered by sub-stages 6b–6g; D-008 still defers those per-area SRVs (SRV-ROUT-*, SRV-RDIR-*, SRV-RWRT-*, SRV-DLST-*, SRV-FILE-003). Q-003 (validation error format) stays open per D-002.
Impact: IrServe now reads `serve.json` / `now.json#now.static` / `package.json#static` per the SRV-CFG-001 lookup chain, accepts `-c/--config <PATH>` to override the implicit chain, exits non-zero on malformed JSON or missing explicit `--config`, and applies the `public` field to the served root. Per-field behavior beyond `public` is parsed-and-stored, not yet observable. Bug-for-bug AJV error wording is excluded by D-002. The reference's TypeError on a `now.json` lacking a top-level `now` key is replaced by clean fall-through to `package.json`; this micro-divergence is authorized by D-004.

## D-010: Stage 6b un-defers routing normalization (multi-slash + trailingSlash)

Date: 2026-05-09 (amended after Codex review rounds 1 and 2 to reflect the as-implemented wiring)
Affected requirements: SRV-ROUT-003, SRV-ROUT-004, SRV-ROUT-005 (un-deferred from D-008's list)
Status: adapted
Reason: Change `004-route-normalization` (Stage 6b) wires the routing-normalization layer of the 13-phase dispatcher. The implementation mirrors `serve-handler/src/index.js:121-185, 561, 586` rather than treating phases 3 and 5 as independent steps: the URI path is percent-decoded once at dispatcher entry; the trailingSlash decision (SRV-ROUT-003, SRV-ROUT-004) is taken on the **uncollapsed** decoded path so the multi-slash override at `index.js:158-160` is observable; the silent multi-slash collapse (SRV-ROUT-005, Q-006 closed) runs only for the path that flows into the resolve stage. When `trailingSlash` is set and the decoded path contains `//`, the redirect target is the slash-collapsed form (`encodeURI`-encoded for the `Location` header, mirroring `index.js:586`), regardless of what the add/strip branches would otherwise compute. Two pure-isolation probes (`trailingslash-add`, `trailingslash-strip`) exercise the redirect surface with `cleanUrls: false` so the cleanUrls 301 (Stage 6c) cannot mask or compose with phase 5; the compose probes (`prec-cleanurls-trailing`, `prec-cleanurls-trailing-false`) and the cleanUrls-dependent anchors of `multislash-collapse` (`double_slash_segment`, `internal_double_slash`) stay reference-only — they are skipped or marked `divergent` in `runner.l0` until Stage 6c lands phase 4. SRV-ROUT-003's oracle list grows by ORC-068, ORC-069, ORC-072, ORC-073, ORC-075, ORC-076; SRV-ROUT-004's by ORC-070, ORC-071, ORC-074; SRV-ROUT-005's by ORC-072, ORC-073, ORC-074. SRV-ROUT-006 (precedence) stays deferred because phases 4, 6, 7, 8 are not yet implemented.
Impact: IrServe now (a) percent-decodes the URI path at dispatcher entry (`crates/irserve-core/src/dispatch.rs`); (b) emits a 301 with the slash-collapsed form when `trailingSlash` is set and the decoded path contains `//` (literal or via `%2F%2F`), overriding the add/strip targets; (c) otherwise emits the trailingSlash 301 to `path + "/"` (add) or to `path[..len-1]` (strip), with dotfile/extension exemptions on the add branch only — matching the reference's `path.parse(p).{name, ext}` shape verbatim, including dotfile-with-extension cases like `/.bashrc.bak` (exempt because the basename has a non-leading dot); (d) silently collapses `//` for the resolve-and-onwards path when `trailingSlash` is unset (silent contract preserved per Q-006 / ORC-025); (e) sets the `Location` header through an `encodeURI`-equivalent encoder (`encode_uri_target` in `dispatch.rs`) so spaces become `%20`, non-ASCII bytes become percent-encoded UTF-8, and reserved-but-safe chars (`?`, `=`, `&`, `:`, `@`, `+`, `$`, `,`, `#`, `/`, `;`) pass through untouched. axum/hyper's auto-emitted `content-length: 0` on the empty 301 body is masked via `runner.l0.contentLengthMayDiffer` since the reference's Node http response omits it; both responses have identical (zero) body bytes. The remaining routing SRVs (SRV-ROUT-001/002 cleanUrls, SRV-ROUT-006 precedence) stay deferred per D-008.

Deferred to Stage 6f (SRV-SEC-001 / SRV-SEC-002, not 6b's scope): malformed percent-escape rejection. The dispatcher uses `percent_encoding::percent_decode_str(...).decode_utf8_lossy()`, which preserves invalid `%xx` sequences (e.g. `%zz`) as literal bytes rather than producing the reference's `400 Bad Request`. As a consequence, when `trailingSlash` is set, a request like `GET /bad%zz` flows through phases 5 and 9–13 unchanged: phase 5's add branch produces `/bad%zz/` (basename has no extension and isn't a dotfile), `encode_uri_target` re-encodes the literal `%` to `%25`, and the response is `301 Location: /bad%zz/` percent-escaped to `/bad%25zz/`. The reference rejects the same request with 400 (its `decodeURIComponent` raises `URIError`, which the handler maps to `400 Bad Request`). This divergence is contracted by ORC-041 (`tools/probe/cases/traversal-raw-encoded.json#raw_malformed_percent`, status: verified, must-match: 400) which intentionally has no `runner.l0` block and therefore auto-skips under `target=irserve`. Promotion to a strict decoder (e.g. `percent_decode_str(...).decode_utf8()` plus a 400 short-circuit on `Err`) and adding the `runner.l0` block to that probe are SRV-SEC-001 work for Stage 6f, not 6b. Likewise, the `traversal-encoded` probe stays reference-only until SRV-SEC-002 (single-pass-decode + dot-segment containment) is wired in 6f.

## D-011: Stage 6c un-defers cleanUrls (phases 4 + 8)

Date: 2026-05-09 (amended after Codex review rounds 1 and 2 to reflect the as-implemented array-form semantics)
Affected requirements: SRV-ROUT-001, SRV-ROUT-002 (un-deferred from D-008's list); SRV-ROUT-006 (partially un-deferred — cleanUrls↔trailingSlash composition observable; cleanUrls↔redirects/rewrites stays deferred until 6e)
Status: adapted
Reason: Change `005-clean-urls` (Stage 6c) wires phases 4 and 8 of the 13-phase dispatcher. Phase 4 (`crates/irserve-core/src/clean_urls.rs::compute_clean_urls_redirect`) emits a 301 to the extension-stripped form for any request matching the end-anchored regex `(\.html|\/index)$` when `cleanUrls` is enabled, with the `//` collapse on the stripped target and `ensureSlashStart` re-prepend mirroring `serve-handler/src/index.js:121-143` exactly. Single-pass strip semantics (`/index.html` → `/index`, NOT `/`) match the reference's regex with the redundant `g` flag against an end-anchor, pinned by the existing `_smoke#index_html_redirect` snapshot. Phase 8 (`crates/irserve-core/src/clean_urls.rs::try_clean_urls_resolve`) tries `<P>/index.html` first and `<P>.html` second, mirroring `findRelated` + `getPossiblePaths('.html')` at `index.js:276-307`; index-first order is probe-confirmed (Q-005 closed by `prec-cleanurls-default`).

The dispatcher gates phase 8 on `url_path_has_extension(&url_path)` so the reference's pre-stat behavior at `index.js:608-642` is faithfully reproduced: extensionless paths skip pre-stat and run phase 8 first (so an existing `<P>.html` is preferred over a bare extensionless `<P>` file, matching SRV-ROUT-006's "no pre-stat for extensionless" clause); has-extension paths run `resolve()` first and only fall back to phase 8 on `NotFound`, preventing `<P>.html` from shadowing an existing `<P>` file. Both phase 4 and phase 8 honor scope via a precompiled `CleanUrlsView` (`Off | On | Scoped(Vec<ScopedPattern>)`) built once at server start from `serve.json#cleanUrls`; each `ScopedPattern` carries a compiled `globset::GlobMatcher` (built with `literal_separator(true)` so `*` does not cross `/`) plus a `negate: bool` for minimatch-style `!`-prefix negation. `globset` was added at workspace pin `0.4` (latest stable verified via context7 on 2026-05-09). `applicable` collapses `//` in the request path before matching (mirrors `path.posix.resolve(requestPath)` inside `sourceMatches` at `index.js:38-67`) and iterates `matcher.is_match(path) ^ negate`, returning `true` on the first truthy result. Invalid glob patterns are silently skipped with a stderr warning emitted from `server.rs::serve` (server keeps running), mirroring the reference's silent-never-match behavior at `index.js:38-67` via `minimatch`.

Compose probes deferred at 6b per D-010 (`prec-cleanurls-trailing.json`, `prec-cleanurls-trailing-false.json`, and the `double_slash_segment` / `internal_double_slash` anchors of `multislash-collapse.json`) flip to L0-clean in this change, closing the cleanUrls↔trailingSlash composition surface. The cleanUrls↔redirects compose surface stays deferred to 6d (`006-configured-redirects`); cleanUrls↔rewrites to 6e (`007-configured-rewrites`). Until those land, SRV-ROUT-006 is `partially un-deferred` — its trailingSlash composition is verified against irserve, its redirects/rewrites composition is not.

`tools/probe/run.mjs::applyL0Filter` was extended: the `bodyMayDiffer` mask now also strips `body.kind`. Reason: raw-mode probes capture HTTP/1.1 chunked-encoding terminators (`0\r\n\r\n`, 5 bytes binary) as a "binary" body against Node's reference, while irserve's hyper layer uses `Content-Length: 0` for the same empty 301 — a transport-encoding choice, not an application-body divergence. Without the extension, the chunked-encoding tail trips the body-kind comparison on `multislash-collapse#double_slash_segment` and `#internal_double_slash`. The semantic widening is "body content (kind, length, bytes, preview) may differ; only headers and status are contractual at L0". The only pre-existing user (`notfound-shape#missing_html`) already had matching `text` kind on both sides, so stripping `kind` is a no-op there.

Impact: IrServe now (a) emits 301 to the extension-stripped form for `.html`, `/index`, and `.../index.html` requests when `cleanUrls` is enabled; (b) resolves extensionless requests via the index-first `<P>/index.html`-then-`<P>.html` candidate chain, with array-form `cleanUrls` globs gating both behaviors; (c) honors the reference's pre-stat ordering so `/foo.css` with both `/foo.css` and `/foo.css.html` present serves the existing `/foo.css` and does not get shadowed; (d) participates correctly in the cleanUrls↔trailingSlash compose surface (phase 4 fires before phase 5, mirroring `index.js:130-133`'s "strip the HTML parts before handling the trailing slash" coupling). The remaining routing SRVs (SRV-ROUT-006 redirects/rewrites composition) stay deferred per D-008 until 6d/6e land. README's "What is NOT yet observable" list is shrunk by cleanUrls; "Try IrServe" is refreshed with cleanUrls examples. ORC-002/012/013/014/017/020/021/022/023/024/026/027 flipped from "verified against reference only" to "verified against both reference and irserve" without renumbering.

Codex review round 1 amendments (P1 + P2): the array-form scope check was tightened against minimatch semantics — globs are now built with `GlobBuilder::new(...).literal_separator(true)` so a single `*` does not cross `/`, and `applicable` collapses `//` in the request path before `is_match` (mirrors `path.posix.resolve(requestPath)` inside `sourceMatches` at `index.js:38-67`). `oracle-matrix.md` ORC-017's must-match field was corrected from `Location: /about/` to `Location: /about` to match the canonical snapshot at `tools/probe/snapshots/prec-cleanurls-trailing.json#about_html` (cleanUrls strip wins over the trailingSlash add per the phase-4-before-5 coupling).

Codex review round 3 amendments (P1 + P2):
- **P1 — extglob (`+(...)`, `@(...)`, `?(...)`, `*(...)`, `!(...)`)
  scoped explicitly out.** The reference's `sourceMatches` calls
  `minimatch` at `index.js:59`, which honors Bash-style extended
  glob constructs. `sourceMatches` is shared between `applicable`
  (cleanUrls scope, `index.js:265`) and `toTarget` (redirects/rewrites,
  `index.js:70`), so extglob support is structurally identical on
  both branches. The pinned reference's only dedicated extglob
  test exercises redirects (`serve-handler/test/integration.test.js:449`,
  `redirects: ["face/+(mask1|mask2)/ideal"]`); the cleanUrls array
  test at `integration.test.js:705` uses a plain `/directory**`
  glob. cleanUrls-side extglob is therefore structurally implied
  but not upstream-tested — captured by the new
  `tools/probe/cases/cleanurls-extglob.json` snapshot in this repo.
  IrServe's `globset::GlobBuilder` does NOT support extglob and as
  of context7 lookup on 2026-05-09 no surveyed Rust crate
  (`globset 0.4.18`, `fast-glob 1.0.1`, `glob-match 0.2.1`,
  `wax 0.7.0`) does either. Rather than pull in regex-translation
  machinery for a power-user surface no committed probe exercises,
  the divergence is recorded as **Q-012** (open) with a
  reference-only probe `tools/probe/cases/cleanurls-extglob.json`
  capturing the gap: `cleanUrls: ["/public/+(page|other).html"]`,
  `GET /public/page.html` → reference 301 → `/public/page`, irserve
  serves the file directly (cleanUrls scope check misses because
  globset treats the extglob constructs as literal characters). The
  array-form contract claim in `005-clean-urls/specs/routing/spec.md`
  reads "standard globs" — `*`, `**`, `?`, character classes
  `[...]`, brace alternation `{a,b}`, and `!`-prefix negation —
  not full minimatch parity. Future closure of Q-012 (option a:
  manual regex translation; option b: a future Rust crate with
  extglob; option c: explicit `adapted` D-NNN) is out of 6c's
  scope.
- **P2 — round-2 stale text fixed.** The first paragraph of D-011
  (above) and the §1 module map + §3 pseudocode in
  `openspec/changes/005-clean-urls/design.md` referenced the
  pre-round-2 shape (`Mode::Scoped(GlobSet)`,
  `Result<Self, globset::Error>`, `GlobSetBuilder`,
  `Error::CleanUrlsGlob` startup-error). Updated in round 3 to
  match the as-implemented shape (`Mode::Scoped(Vec<ScopedPattern>)`,
  `(Self, Vec<InvalidGlob>)`, per-pattern `GlobMatcher` with
  `negate: bool`, silent-skip + stderr warning). The round-2
  amendment paragraph below was already correct; the stale
  references in the original D-011 paragraph were what tripped
  Codex.

Codex review round 2 amendments (P1 + P2): two more reference-faithfulness gaps closed.
- **Negation patterns now honored.** `slasher` preserves a leading `!` (mirrors `glob-slash.js:8`); `Mode::Scoped` stores `Vec<ScopedPattern>` where each pattern carries a `negate: bool`; `applicable` evaluates `matcher.is_match(path) ^ negate` per pattern and returns `true` on the first true result (mirrors minimatch's per-pattern negate with `nonegate: false` plus the reference's `applicable` iteration at `index.js:261-268`). A sole `!/secret/**` therefore enables cleanUrls for every path outside `/secret/**`, matching the reference. Backed by ORC-077 (`cases/cleanurls-negation.json`, 4 anchors).
- **Invalid globs are silent, not fatal.** `CleanUrlsView::from_config` was changed from `Result<Self, globset::Error>` to `(Self, Vec<InvalidGlob>)`: unparseable patterns are collected into the warnings vector, server startup continues, and the bin layer (`server.rs::serve`) emits one stderr warning per skipped pattern. The `Error::CleanUrlsGlob` variant was removed. This mirrors the reference's behavior at `index.js:38-67` via `minimatch` (unparseable patterns silently never match; server keeps running). Backed by ORC-078 (`cases/cleanurls-invalid-glob.json#html_no_redirect`).

## D-012: Stage 6d un-defers configured redirects (phase 6)

Date: 2026-05-09
Affected requirements: SRV-RDIR-001, SRV-RDIR-002, SRV-RDIR-003 (un-deferred from D-008's list); SRV-ROUT-006 (further un-deferred — trailingSlash↔redirects composition observable; cleanUrls↔redirects composition pinned via existing `prec-rewrites-redirects#page_html_cleanurl_default` anchor; redirects↔rewrites stays deferred until 6e)
Status: adapted
Reason: Change `006-configured-redirects` (Stage 6d) wires phase 6 of the 13-phase dispatcher. Source patterns route through three compiled matchers in `crates/irserve-core/src/redirects.rs`: `Literal` (no glob meta, no `:name`, no `*`; trailing-slash flexion mirrors `pathToRegExp("/old", []) = ^/old/?$`), `Glob` (sources with `?`/`[`/`{` glob meta and no `*`/`:name`, OR any `!`-prefixed source — compiled via `globset::GlobBuilder` with `literal_separator(true)`; `!`-prefix negation through the same XOR pattern as cleanUrls), and `Pattern` (sources with `:name` segments OR `*` tokens that aren't `!`-prefixed, compiled into a `regex::Regex` with named capture groups, mirroring `serve-handler/src/index.js:46-49`'s `slashed.replace('*', '(.*)')` + `pathToRegExp` first-pass). The `*`-bearing-source routing was tightened in Codex round 1 P1 from "globset, single segment" to "regex, cross-segment" so that `/dir/*` matches both `/dir/page` and `/dir/sub/page`, matching the reference (the original draft of D-012 documented the divergence as intentional; Codex flagged that as an undocumented behavior gap and the routing was fixed instead). Pinned by ORC-084/ORC-085. Codex round 2 P1 added two refinements to `compile_source_regex` (later refined again by round 3 P1): (a) only the FIRST `*` becomes `(.*)`, mirroring JS `String.prototype.replace('*', '(.*)')` — subsequent `*`s become regex literals so the regex itself doesn't over-match (round 3 then added the glob fallback so multi-`*` sources still match real URLs single-segment-per-`*` via minimatch parity); (b) consecutive `*`s collapse to a single one, so `/dir/**` still matches like `/dir/*` (ORC-089/ORC-090) — mirroring the reference's effective `**`-handling at `serve-handler/test/integration.test.js:432`. Codex round 2 P1 also reversed the `CompileError::NegatedParam` rejection: `!`-prefix combined with `:name` now routes to `Glob` with `negate=true`, mirroring the reference's minimatch fallback (`:name` fragments are treated as literal characters; the rule fires for every path that does NOT literally equal the slasher-normalized source). Pinned by ORC-094. The destination is pre-parsed into a `DestTemplate` of literal + `:name` fragments; per-value `encodeURIComponent` is applied at render time, mirroring `pathToRegExp.compile`'s per-prop encoding (`index.js:81-87`). The surrounding `encodeURI` over the full target lives in `dispatch::encode_uri_target` (already existed since 6b). `compute_configured_redirects` walks the rules in order and returns the first match's `(target, status_code)`; status defaults to 301 and uses the rule's `type` field when present, mirroring `index.js:179`. Out-of-range or otherwise invalid `type` values fall back to 301 (matches "accept any 3xx; range-checking is not specified" per `openspec/specs/redirects/spec.md` SRV-RDIR-002 Note). `compile_rules` returns `(Vec<RedirectRuleCompiled>, Vec<InvalidRedirect>)` and the bin layer emits one stderr warning per skipped rule, mirroring the cleanUrls treatment from 6c. Workspace pin `regex = "1"` (latest stable 1.12, verified via context7 on 2026-05-09).

Codex review round 3 P1 added a `glob_fallback: Option<GlobMatcher>` field to the `Pattern` matcher for `*`-bearing sources, mirroring the reference's `sourceMatches` two-stage matching at `index.js:46-67`: try `pathToRegExp` first, fall back to `minimatch` on null. With `path-to-regexp@3.3.0` not recognizing bare `*` as a wildcard token, multi-`*` sources like `/a/*/b/*` produce a regex with literal `\*` characters that real URLs never match — the glob fallback recovers minimatch's single-segment-per-`*` behavior. Pinned by `redirects-glob-source.json`: positive case `/a/x/b/y` matches (ORC-095), negative cases stay 404 (ORC-091 multi-segment trailing, ORC-096 no trailing segment, ORC-097 extra middle segment). The fallback trims a single trailing `/` from the path before matching (mirrors `path.posix.resolve` in `index.js:41`) so that globset's zero-or-more `*` semantics line up with minimatch's one-or-more.

Codex review round 4 added two refinements to the glob fallback. **P1 (leading-dot rejection):** minimatch's default is `dot: false`, where a `*` pattern segment does NOT match a path segment beginning with `.`. globset matches dotfiles, so we needed a post-validation step. **P2 (`:name`+`*` fallback):** the round-3 implementation gated `glob_fallback` construction on `!has_path_param(&body)` based on the assumption that minimatch would never match for `:name`-bearing sources (because `:name` would never appear literally in a path). Codex showed that contrived but valid requests can carry literal `:name` segments (URL paths permit `:` unencoded), in which case the reference's minimatch fallback DOES fire and emit a 301. The gate was removed so `glob_fallback` builds for any `*`-bearing source. Pinned by ORC-100 (`/a/:id/x/b/y` against `/a/:id/*/b/*` returns 301 via the fallback).

Codex review round 5 P1 generalized the dot-rejection rule. The round-4 `matches_strict` was both too strict and too loose: it rejected only `*`-bearing pattern segments and bypassed `**` entirely (`has_doublestar` skipped all validation). The actual minimatch rule is: *a path segment beginning with `.` matches only when the pattern segment itself begins with a LITERAL `.` — regardless of which other glob meta-characters the segment contains*. So `.*` (literal dot then `*`) admits `.x`; `[.]y` (begins with magic `[`) does NOT admit `.y` even though the bracket class contains `.`; `**` (begins with magic `*`) does NOT admit dot segments in its expansion. The fix replaced the bare `Option<GlobMatcher>` with a per-segment `Vec<PatSeg>` (Literal | Wildcard{matcher, starts_with_dot} | DoubleStar) and a recursive `match_segments` walker. The recursion handles `**` expansion (zero or more segments, with each consumed segment subject to the dot rule) precisely. Pinned by ORC-101 (`.*` admits `.x`), ORC-102 (`[.]y` rejects `.y`), ORC-103 (`/gs/**/b/*` rejects `/gs/.x/b/y`).

Codex review round 6 surfaced three more divergences. **P1 (Glob bypass):** the round-5 segment matcher applied only inside `Pattern`'s `glob_fallback`. `Matcher::Glob` (sources with `?`/`[`/`{` glob meta but no `*`/`:name`) still ran raw `globset.is_match()` and missed the per-segment dot rule. Repro: `/g/[.]y` against `/g/.y` returned 301 in irserve while reference returned 404. Fix: `Matcher::Glob` now stores `Vec<PatSeg>` and runs the same `match_segments` walker. Negation `!`-prefix XOR continues to apply on top, so `!/g/[.]y` against `/g/.y` correctly returns 301 (the inner pattern rejects `.y`, the negation flips). Pinned by ORC-104 (positive bracket reject), ORC-108 (negation flip). **P1 (brace before dot rule):** `classify_pattern_segment` set `starts_with_dot` from the raw segment — so `{.x,y}` was treated as not dot-starting because the segment begins with `{`. Minimatch expands braces BEFORE applying the dot rule, so the `.x` alternative legitimately admits leading-dot paths. Fix: `segment_can_start_with_dot` walks brace alternatives recursively; any alternative beginning with literal `.` flips the flag. Pinned by ORC-105 (`{.x,y}` admits `.x`) and ORC-106 (control: `{x,y}` rejects `.x`). **P2 (`:name`+non-`*` glob fallback):** `glob_fallback` was gated on `body.contains('*')`, so `:name`+`?` or `:name`+`{...}` sources had no minimatch fallback. The reference's `sourceMatches` always tries minimatch on path-to-regexp miss, regardless of source shape. Fix: build `glob_fallback` for every Pattern matcher. Pinned by ORC-107 (`:id`+`?` literal match via fallback).

Codex review round 7 surfaced three more corners. **P1.1 (path.posix.resolve trim):** the reference applies `path.posix.resolve(requestPath)` BEFORE both `pathToRegExp.exec` and `minimatch` (`index.js:41`). The trim drops a single trailing `/` (except `/` itself). The round-6 implementation only trimmed in the Glob / glob_fallback branches; the Pattern regex saw the raw path, so `/a/*` against `/a/` matched in irserve while reference returned 404 (after trim, regex needs `/(.*)` which `/a` can't satisfy). Fix: trim once at the top of `try_match`. Pinned by ORC-109 (`/a/*` against `/a/` 404), ORC-110 (`:name`+`*` against `/p/foo/` 404). **P1.2 (`**` after param under-matches):** path-to-regexp v3 parses the trailing `*` after `(.*)` as an `*` modifier on the segment, making it optional+repeat — so `/a/:id/(.*)*` matches `/a/foo` (zero trailing segments) with `id=foo`. The round-5 compiler collapsed `**` into the same first-only `(.*)` substitution, requiring the extra slash segment, and the glob fallback couldn't rescue realistic paths because it treats `:id` literally. Fix: refactor `compile_source_regex` to be segment-aware. `**` segments now emit `(?:/(.*))?` (optional multi-segment) instead of mandatory `/(.*)`. Pinned by ORC-111 (`/a/:id/**` admits `/a/foo`), ORC-112 (consumes multi-segment trailing), ORC-113 (`**` middle admits zero before literal `/z`). **P1.3 (DoubleStar end-of-pattern strict in segment matcher):** the round-5 `match_segments` allowed `DoubleStar` to consume zero segments universally. Empirically minimatch (per `index.js:59`) requires `**` at the END of a pattern to consume at least ONE segment — `/a/**` does NOT match `/a` via minimatch, even though it does match via pathToRegExp's `(.*)*`. The asymmetry shows up via negation: `!/a/**` falls through to minimatch (because `!`-bearing pattern fails pathToRegExp) and the inner pattern's strict semantics determine the negation result. Fix: in `match_segments`, when `**` is the last pattern element (`pat.len() == 1`), require `min_skip = 1` and reject if path is empty. `**` in middle still allows zero-skip (verified by empirical probe of `!/a/**/b` against `/a/b` → 404, meaning inner matched). The asymmetry between positive (regex, permissive) and negation (segment matcher, strict) routes mirrors the reference's pathToRegExp-vs-minimatch divergence. Pinned by ORC-114 (`!/a/**` admits `/a`), ORC-115 (`/a/` resolved same), ORC-116 (negation rejects `/a/x`), ORC-117 (negation matches `/b`).

Codex review round 8 closed two more parity gaps. **P1 (full `path.posix.resolve`):** the round-7 trim only stripped a trailing `/`; reference's `path.posix.resolve` ALSO resolves `.` / `..` segments and collapses consecutive slashes. Raw-mode requests like `GET /a/./x` and `GET /a/b/../x` resolve to `/a/x` in reference, matching a literal `/a/x` rule. irserve was returning 404 because the dot/dotdot segments survived to the matchers. Fix: replace the simple trim with a full `path_posix_resolve` (uses `path_posix_normalize` + trailing-slash trim). Pinned by ORC-118 (`.` segment), ORC-119 (`..` segment), ORC-120 (`.` + `:name`). **P2 (`\\` escape in source patterns):** the round-8 fix (a) `de_escape`-d Literal source bodies, (b) enabled `GlobBuilder::backslash_escape(true)` for Wildcard segments, and (c) added a `\.`-prefix check in `segment_can_start_with_dot`. Pinned by ORC-121 (literal-`\.x`), ORC-122 (Wildcard `\.*` admits dotfile).

Codex review round 9 P1 corrected the round-8 over-generalization. Empirical testing of `minimatch@3.1.5` (the version pinned by serve-handler) showed that `\X` is effectively transparent for X in `*`/`?`/`[`/`{` — the backslash is stripped but the meta-character keeps its glob meaning. So `/s/\*` matches `/s/foo` (the `*` is still wildcard); `/q/\?` matches `/q/a`; `/br/\[ab]` matches `/br/a`; `/bc/\{a,b}` matches `/bc/a`. Round 8's `backslash_escape(true)` made globset interpret `\*` as a literal `*`, under-matching real paths. The round-9 fix is uniform: `de_escape` in `classify_pattern_segment` strips `\` from each segment before classification, so `\.x` becomes `.x` (Literal), `\.*` becomes `.*` (Wildcard, dot rule allows leading-dot via natural `.`-prefix), and `\*`/`\?`/`\[`/`\{` become plain `*`/`?`/`[`/`{` (Wildcard with regular glob meta). The round-8 `\.`-prefix check in `segment_can_start_with_dot` was removed as redundant — the de-escaped segment naturally hits the `.`-prefix branch. Pinned by ORC-123 (escaped-`*`), ORC-124 (escaped-`?`), ORC-125/126 (escaped-`[ab]`), ORC-127 (escaped-`{a,b}`).

Codex review round 10 closed three more parity gaps. **P1.1 (escaped globstar):** the `seg == "**"` check in `classify_pattern_segment` ran BEFORE de-escape, so source `\**` was classified as a single-segment Wildcard with literal `*` content rather than as DoubleStar. Empirically `minimatch('/a/x/y', '/a/\**')` returns true — minimatch parses `\**` as globstar. Fix: move de-escape before the `**` check. The closely-related `\*\*` case (which minimatch parses as TWO single-segment globs, NOT globstar — `minimatch('/b/x', '/b/\*\*')` is false but `/b/x/y` is true) is documented as a known divergence; mirroring exactly would require a custom minimatch parser that distinguishes the escape positions of consecutive stars, which is out of 6d's scope. Pinned by ORC-128. **P1.2 (per-alt dot rule for braces):** the round-6 implementation tracked a single `starts_with_dot: bool` per Wildcard segment, set to true when ANY brace alternative began with `.`. That over-permitted: `{.x,*}` admitted any dotfile because globset's `*` alt matches dotfiles, even though minimatch only allows the dot-prefixed alt to do so. Fix: replace `starts_with_dot` with `dot_matcher: Option<GlobMatcher>` — `Some(matcher)` when at least one alt begins with `.`, containing a globset matcher built from ONLY those dot-prefixed alts. At match time, dotfile paths route to `dot_matcher` (rejected if it's `None`); non-dot paths use the full matcher. New `collect_dot_starting_alternatives` walks brace alts recursively to extract them. Pinned by ORC-129 (dot alt admits `.x`), ORC-130 (`*` alt admits `abc`), ORC-131 (other dotfile rejected). **P2 (globset-error fallback to Literal):** the round-9 unified de-escape produces `[` for source `\[`, which globset rejects as an unmatched bracket — the rule was being silently dropped. Reference matches such patterns literally (minimatch produces a regex that matches the literal `[`). Fix: in `classify_pattern_segment`, on globset compile error, fall back to a Literal segment with the de-escaped form. Pinned by ORC-132 (`/u/\[` literal-matches `[`). Two known divergences remain documented: `\*\*` (per-alt parsing) and `{a\,b,c}` brace-with-escaped-comma (minimatch's quirky regex with `\/` separators) — both rare patterns where mirroring requires a fuller minimatch port.

Codex review round 11 closed the remaining backslash-vs-`%5C` request-path corner. **P1 (trailing `\` and inner `\X` in Literal sources, plus glob_fallback over-match for `*`-bearing sources with trailing `\`):** the round-8 `de_escape` silently dropped a trailing unescaped `\`, and the per-segment `de_escape` in `classify_pattern_segment` did the same for the Pattern matcher's glob_fallback. Three concrete divergences against requests with literal backslashes (decoded from `%5C`): (a) source `/u/foo\` matched `/u/foo` (over-match) but failed to match `/u/foo\` (under-match); (b) source `/u/\f` matched `/u/f` only, missing minimatch's segment-level transparency that empirically matches `/u/\f` too (`minimatch('/u/\\f', '/u/\\f') === true`); (c) source `/v/*\` over-matched `/v/x` via the glob_fallback (segment `*\` de-escaped to bare `*`). Fix: the Literal variant now stores TWO match forms — `source_ptr` (de-escape with trailing `\` PRESERVED, mirroring path-to-regexp's `(\\.)`-then-literal-accumulator behavior in v3.3.0) and `source_mm: Option<String>` (the raw body, populated when the body has `\` AND no trailing unescaped `\`, mirroring minimatch's segment-level transparency). For the Pattern matcher's glob_fallback: a new helper `ends_with_unescaped_backslash` gates the fallback off when the source body ends with an odd number of trailing backslashes; minimatch's compiled regex requires a synthetic trailing `/` suffix that `path.posix.resolve` always strips, so the fallback can never match a resolved path anyway. The Pattern's primary regex was already correct (it preserves the trailing `\` as a literal `\` in its compiled regex), so requests with literal trailing `\` still match. New helper `de_escape_keep_trailing` is alongside the existing `de_escape` (the latter retained for per-segment classifiers in Glob and the now-gated glob_fallback). Pinned by ORC-133 (literal source, no trailing `\`, must NOT match), ORC-134 (literal source, request with `%5C` end, MUST match), ORC-135 (inner-escape de-escaped form), ORC-136 (inner-escape raw form), ORC-137 (glob source, no trailing `\` request, must NOT match), ORC-138 (glob source, `%5C` request, MUST match). Round 11 also fixed stale comments in `server.rs` and `proposal.md` that still claimed invalid glob sources are "dropped" — round 10's globset-error fallback to Literal means sources like `/u/\[` are now recovered, not dropped.

Codex review round 12 closed the case-insensitivity gap on the path-to-regexp branch. **P1 (default `i` flag):** `path-to-regexp@3.3.0` ships its compiled regex with the `i` flag by default — empirically `pathToRegExp('/Case').flags === 'i'`. Source `/Case` matches request `/case`; `/P/:id` matches `/p/Foo`; `/S/*` matches `/s/x`. The previous IrServe implementation was case-sensitive throughout. Fix: `literal_matches` takes a new `case_sensitive: bool` parameter; the Literal arm of `try_match` calls it with `false` for `source_ptr` (path-to-regexp branch) and `true` for `source_mm` (minimatch branch). `compile_source_regex` builds the Pattern matcher's regex via `regex::RegexBuilder::case_insensitive(true)`. The Glob matcher and the Pattern matcher's `glob_fallback` remain case-sensitive — minimatch's default is `nocase: false`, and both routes mirror minimatch. Pinned by ORC-139 (Literal lowercase request), ORC-140 (Literal same-case control), ORC-141 (Pattern `:name` case-insensitive plus capture-value preservation), ORC-142 (Pattern `*` case-insensitive), ORC-143 (Glob-only control proving minimatch case-sensitivity is preserved). **P2 (segment-internal trailing `\` in non-final glob segments) — pushed back as Windows-specific platform divergence.** Codex's reproduction `source: "/g/?\\/bar"` against request `/g/a%5C/bar` showed reference matches but IrServe doesn't. Empirical investigation (`Minimatch.matchOne` returns false directly; `m.match()` returns true) traced the divergence to `minimatch.js:742-745`: `if (path.sep !== '/') { f = f.split(path.sep).join('/') }`. This is filesystem-aware behavior — minimatch on Windows treats `\` as a path separator and converts it to `/` BEFORE segment matching. On Linux the same source/request is 404 in reference too. Mirroring would require `cfg!(target_os = "windows")` conditional code that creates platform-divergent test behavior (same source/request 404s on Linux but 301s on Windows). The case is also narrow: a non-final glob segment with a trailing `\`. Documented as a known divergence alongside `\*\*` (per-alt parsing) and `{a\,b,c}` (brace-with-escaped-comma). If a future user reports this divergence, the fix would add a Windows-only `\` → `/` normalization in `match_segments`, scoped to the platform and clearly labeled.

Codex review round 13 refined the case-folding semantics from round 12. **P1 (Latin-1 vs special-fold asymmetry):** the round-12 implementation used `eq_ignore_ascii_case` for `Literal.source_ptr` (ASCII-only folding) and `regex::RegexBuilder::case_insensitive(true)` for `Pattern.regex` (Rust's Unicode-default folding). This produced two divergences: (a) Literal under-matched Latin-1 — source `/Ä` missed request `/ä`, while reference matches via JS `/Ä/i.test('ä') === true`; (b) Pattern over-matched Unicode special folds — source `/K` (Kelvin sign U+212A) `/:id` matched `/k/Foo`, while JS `i` flag *without* the `u` flag does NOT apply special folds (`new RegExp('K', 'i').test('k') === false` for Kelvin K). Fix for Latin-1: switch Literal's `eq_ignore_ascii_case` to Unicode-aware `to_lowercase`, matching Rust regex's `case_insensitive(true)` semantics. This unifies Literal and Pattern behavior on the path-to-regexp branch and handles Latin-1 letters (Ä/ä, É/é, Ö/ö, Ü/ü) that real-world German/French/Spanish URLs use. Pinned by ORC-144..147. The residual divergence on Unicode special folds (Kelvin sign U+212A → ASCII `k`, ﬃ ligature → `ffi`, etc.) is accepted as a known divergence — bug-for-bug parity would require shipping a custom JS-specific case-folding table for limited real-world value (URLs almost never contain special-fold codepoints). The trade-off was justified on its own merits (cost-vs-value, narrow scope of divergence). Recorded as a compatibility note on SRV-RDIR-001 in the spec delta and in `inventory.md` so the contract does not promise more than is delivered. Codex round 14 P2 surfaced that the spec delta and inventory had been omitted at first commit; this was corrected in-line.

Q-007 (relative / scheme-relative / absolute Location forms) is closed by snapshot `tools/probe/snapshots/redirects-destination-forms.json` (7 anchors after Codex rounds 1+2: ORC-079 `absolute_https`, ORC-080 `scheme_relative`, ORC-081 `relative_no_leading_slash`, ORC-082 `absolute_path_baseline`, ORC-086 `dotdot_resolved` (mid-path `..`), ORC-087 `dotdot_leading_resolved` (leading `..`), ORC-088 `empty_destination_root`). The reference's destination-side `slasher` is `path.posix.normalize(path.posix.join('/', value))` (`third_party/serve-handler/src/glob-slash.js:6`), called from `index.js:80`'s `protocol ? destination : slasher(destination)`. The Q-007 surprises are that `path.posix.normalize` collapses consecutive slashes (so a scheme-relative `//example.com/x` is normalized to `/example.com/x` and emitted as a same-origin redirect, not as a true scheme-relative URL) AND resolves `.`/`..` segments (so a `destination: "a/../b"` rule emits `Location: /b`). Absolute URLs (`https://example.com/x`) skip normalization and pass through verbatim; relative paths (`foo/bar`) get a leading slash; absolute paths (`/foo/bar`) pass through unchanged. IrServe mirrors via `redirects::normalize_destination` + `redirects::slasher_join_normalize` + `redirects::path_posix_normalize`. Codex review round 2 P1 corrected the `slasher_join_normalize` helper to prepend `/` BEFORE `path_posix_normalize` (not after), so leading-`..` and empty-string inputs resolve correctly: `../b` joins to `/../b` then normalizes to `/b`; `""` joins to `/` then normalizes to `/`. The same `slasher_join_normalize` is used by the source-side `slasher` so source patterns like `../old` resolve to literal `/old` (ORC-092, ORC-093). SRV-RDIR-003 is promoted from `accepted` to `verified`. Q-012 (extglob) limitation is inherited unchanged: redirects glob sources use the same `globset` matcher as cleanUrls and so do not honor `+(...)`, `@(...)`, `?(...)`, `*(...)`, `!(...)` extglob constructs.

The trailingSlash↔redirects compose corner of SRV-ROUT-006 is closed by `prec-trailing-redirects.json` (ORC-083, anchor `trailing_add_wins_over_redirect`): with `trailingSlash: true` and a redirect `/face/mask → /elsewhere`, the request `GET /face/mask` returns `301 Location: /face/mask/` because phase 5 fires before phase 6 (mirrors the order of the `slashing` branch at `index.js:145-168` versus the redirects loop at `index.js:172-182`). The cleanUrls↔redirects compose corner was already covered by `prec-rewrites-redirects.json#page_html_cleanurl_default` (the cleanUrls 301 to `/page` fires; the configured redirect `/go → /target` is unrelated to the page-level path). The redirects↔rewrites compose corner stays deferred until 6e wires phase 7 — `prec-rewrites-redirects.json#go_root` already pins the redirect-wins side from 6d.

Probe-case `runner.l0.clean` blocks added or extended in 6d:
- `redirects-types.json` — `explicit_302`, `default_301_segment` (literal + path-segment; `contentLengthMayDiffer` for both: axum emits `content-length: 0` on empty redirects, the reference's Node `http` does not).
- `prec-rewrites-redirects.json` — `go_root`, `page_html_cleanurl_default` (redirect wins over rewrite; cleanUrls wins over redirect; `contentLengthMayDiffer` for both).
- `redirects-destination-forms.json` — all four anchors (Q-007 closure; `contentLengthMayDiffer` for all).
- `prec-trailing-redirects.json` — `trailing_add_wins_over_redirect` (`contentLengthMayDiffer`).

Impact: IrServe now (a) responds to a `redirects` array in `serve.json` with a 301 (or rule-specified 3xx) to the rendered destination; (b) supports literal sources, standard-glob sources (including `!`-prefix negation), and path-to-regexp-style `:name` sources; (c) interpolates captured values into destinations via `:name` template fragments with `encodeURIComponent` per value plus `encodeURI` over the final target; (d) normalizes destinations exactly like the reference's `slasher`, including the surprising scheme-relative collapse. The remaining redirects-related deferral is the redirects↔rewrites compose corner (6e); SRV-ROUT-006 closes fully when 6e lands. README's "What is NOT yet observable" list is shrunk by configured redirects; "Try IrServe" is refreshed with a redirects example.

## D-013: Stage 6e un-defers configured rewrites + `--single` (phase 7)

Date: 2026-05-10
Affected requirements: SRV-RWRT-001, SRV-CLI-008 (un-deferred from D-008's list); SRV-ROUT-006 (further un-deferred — redirects↔rewrites composition observable on the rewrite-wins side via `single-with-redirect#redirect_wins_over_single`; trailingSlash↔rewrites and cleanUrls↔rewrites composition implicitly closed by phase ordering and the existing `prec-rewrites-redirects` anchors).
Status: adapted
Reason: Change `007-configured-rewrites` (Stage 6e) wires phase 7 of the 13-phase dispatcher. The matcher kernel was extracted from `redirects.rs` into a shared `crates/irserve-core/src/path_pattern.rs` module — `Matcher::compile` / `Matcher::try_match` now serve both phase 6 and phase 7. The redirects code reduces to a thin wrapper that adds the `status_code` extra; rewrites adds nothing on top of the bare matcher. This closes the "factor out into `path_pattern.rs`" hint left in the 6d plan when the third capability appeared. `path_pattern.rs` carries verbatim the Literal/Glob/Pattern routing, `:name` regex compiler, dot-rule, glob-meta classification, backslash and escape handling, default `i` flag, Latin-1 case folding, and `path.posix.resolve` / slasher helpers from 6d — all D-012 known divergences inherit unchanged for rewrites.

`apply_rewrites` mirrors `serve-handler/src/index.js:91-117`'s `applyRewrites` with rule-removal recursion: on match, the matched rule is removed from the active list (Vec<&T> reallocated minus the matched index — O(n²) on rule count, n ≤ 64 cap) and the function recurses on the rendered destination with `repetitive=true`. Termination happens when the active list is empty or no remaining rule matches, returning the path captured by the most recent successful pass. The first call (with `repetitive=false`) returns `None` if no rule matches the original path so the dispatcher falls through to the standard cleanUrls + resolve chain. The reference applies `slasher(target)` before recursing; IrServe does NOT re-slasher because `Matcher::compile`'s `normalize_destination` already runs slasher on destinations at compile time, and `compile_dest_template`'s capture rendering applies `encodeURIComponent` per value (which encodes `/` to `%2F`), so captured segments cannot reintroduce path separators. For all probe-pinned cases the recursive slasher would be a no-op. If a future probe surfaces a divergence (e.g. a captured `..` from a literal-`:name` request), the fix is a one-line `slasher_join_normalize` on the recursion arm.

Phase 7 wiring respects the pre-stat asymmetry mandated by `openspec/specs/rewrites/spec.md:21-29` and mirrored from `serve-handler/src/index.js:608-642`: has-extension paths whose file exists short-circuit (rewrites NEVER consulted); has-extension paths whose file is missing apply rewrites and prefer the rewritten path's resolution over `<P>.html` cleanUrls candidates (mirrors `findRelated`'s `rewrittenPath ? [rewrittenPath] : getPossiblePaths(...)`); extensionless paths apply rewrites unconditionally. The dispatcher implements this inside the existing extension-split fork from 6c, replacing only the previous `// Phase 7` stub with a fork-aware block.

The `--single` SPA fallback (SRV-CLI-008) mirrors `third_party/serve/source/main.ts:78-90` exactly: `crates/irserve/src/main.rs` adds the `-s`/`--single` clap flag and, when set, prepends `RewriteRule { source: "**", destination: "/index.html" }` to `serve_config.rewrites` BEFORE `compile_rewrite_rules` runs. The synthetic rule participates in the standard phase-7 pipeline. Three contractual consequences: (1) configured redirects (phase 6) still win — `single-with-redirect#redirect_wins_over_single` pins this; (2) the pre-stat asymmetry still applies — `single-flag#spa_with_existing_html` (with cleanUrls disabled in the fixture to isolate phase-7 behavior) pins this; (3) prepend-position shadows user rewrites — under chained matching, the `**` catch-all matches first, the rule is consumed, and the user rule then receives the rewritten path `/index.html` (which it doesn't match), so user rewrites listed after `--single` are effectively dead. `single-with-rewrites#extensionless_user_path_shadowed_by_single` pins this.

Probe-case `runner.l0.clean` blocks added or extended in 6e:
- `rewrites-segment.json` — `segment_rewrite`, `spa_fallback` (already verified against the reference; promoted to L0-clean in 6e).
- `rewrites-chain.json` (NEW) — `chain_a_to_b_to_c` (recursive chain `/a → /b → /c.html` with only `/c.html` existing; pins D-013's chaining contract).
- `single-flag.json` (NEW) — `spa_root`, `spa_deep`, `spa_with_existing_html` (cleanUrls disabled in fixture to isolate phase-7 behavior).
- `single-with-redirect.json` (NEW) — `redirect_wins_over_single` (`contentLengthMayDiffer` because axum emits `content-length: 0` on empty redirects, the reference's Node `http` does not).
- `single-with-rewrites.json` (NEW) — `extensionless_user_path_shadowed_by_single`.

`tools/probe/run.mjs` had `-s`/`--single` in `L0_DEFERRED_FLAGS` per D-008. Slice 5 of 6e removes that entry; the existing dual `serveArgs` plumbing at `run.mjs:169`/`:173` already passes the flag symmetrically to both reference and irserve targets. No new runner mechanism was needed.

Impact: IrServe now (a) responds to a `rewrites` array in `serve.json` by serving the (potentially-chained) rewritten path with status 200 (no redirect); (b) honors `-s`/`--single` by prepending the synthetic catch-all rewrite at config-load time; (c) implements pre-stat asymmetry per the spec; (d) shares the entire source-pattern matcher with redirects via `path_pattern.rs`. The remaining rewrites-related deferral is L3 MIME fallback when the rewritten file is missing (SRV-RWRT-002). README's "What is NOT yet observable" list is shrunk by rewrites + `--single`; "Try IrServe" is refreshed with rewrites and `--single` examples.

Codex review round 1 amendments (P1 + P2): three more parity gaps closed.
- **P1 (extensionless rewrite-target-miss falls back to ORIGINAL).** The first commit of slice 2 returned 404 when an extensionless rewrite matched but the rewritten file didn't exist. The reference's `findRelated` returns null in that case, but the surrounding `if (!stats)` final lstat at `index.js:634-642` then runs on the ORIGINAL absolutePath — which can succeed for extensionless original files that exist. So `/about` (existing extensionless file) with rewrite `/about → /missing.html` serves `/about` in reference, not 404. Fix in `dispatch.rs`: on `resolve(target) → NotFound | EscapedRoot` for the extensionless branch, fall back to `resolve(url_path)`. The fallback is the ORIGINAL path only — no cleanUrls candidates, mirroring `findRelated`'s `rewrittenPath ? [rewrittenPath] : getPossiblePaths(...)` branch where cleanUrls candidates are consulted only when no rewrite matched. Pinned by ORC-156 (`rewrites-prestat.json#extensionless_miss_falls_back`).
- **P2 (`url_path_has_extension` for trailing-slash paths).** Node's `path.posix.extname('/foo.txt/')` returns `'.txt'` (trailing slashes are trimmed before computing the basename). The first implementation used `rsplit('/').next()` which yields the empty trailing segment, mis-classifying trailing-slash paths as extensionless and letting rewrites fire on directory-like requests where the reference does not. Fix: walk segments with `rsplit('/').find(|b| !b.is_empty())` to skip trailing empties before applying the dot-rule. Six new unit tests in `dispatch.rs::tests` cover root, dotfile, nested-dotfile-with-ext, trailing-slash-with-ext, and trailing-slash-extensionless cases.
- **P2 (rewrite-wins side of redirects↔rewrites compose).** Slice 6's first cut of `routing/spec.md` claimed ORC-152 closes the "rewrite-wins side", but ORC-152 demonstrates redirect-wins (an explicit redirect overrides `--single`'s catch-all rewrite). The actual rewrite-wins side — request matches a rewrite, does NOT match any redirect — was not explicitly pinned. Round 1 adds `prec-redirects-rewrites-rewrite-wins.json#rewrite_fires_when_redirect_doesnt_match` (ORC-158): redirects `[/old → /new]` plus rewrites `[/api → /api.html]`, request `/api` → 200 with `/api.html` body. Spec wording in `routing/spec.md` corrected to attribute ORC-152 to its actual role (`--single`-vs-redirect ordering) and ORC-158 to the compose corner.

Two probes intentionally NOT added (Codex flagged them as planned-but-undelivered): `rewrites-cycle.json` (would pin pathological cycle behavior; the reference's lack of a recursion cap makes the snapshot unstable across implementations — D-014's irserve-only cap is exercised by the `depth_cap_clamps_to_last_path` unit test in `rewrites.rs::tests`) and `rewrites-chain#chain_order_independent` (algorithmically identical to `chain_a_to_b_to_c` — order-independence is already covered by the `chain_order_independent` Rust unit test in `rewrites.rs::tests`). Probe-level coverage for these two would duplicate algorithmic coverage already provided by unit tests.

## D-014: Recursion-depth cap on rewrite chaining (irserve-only)

Date: 2026-05-10
Affected requirements: SRV-RWRT-001 (chaining contract; companion to D-013).
Status: adapted
Reason: The reference imposes no cap on `applyRewrites`'s recursion (`serve-handler/src/index.js:91-117`). A malformed cyclic configuration could in principle stack-overflow V8. IrServe adds a hard cap of 64 in `crates/irserve-core/src/rewrites.rs` (`REWRITE_DEPTH_CAP: usize = 64`) as defense-in-depth. On overflow, `apply_rewrites` returns `fallback` (which for `repetitive=true` calls is `Some(request_path)` — i.e. the path captured by the most recent successful pass). No error response, no panic, no log line.

Justification for 64: real-world `serve` rule counts are 2-3; the splice model consumes one rule per pass, so any non-cyclic configuration terminates in ≤ N passes; intentional self-cycles (`/a → /a`) terminate after one pass because the matched rule is removed; two-rule cycles (`/a → /b`, `/b → /a`) terminate after both rules have fired once. The cap is unreachable in any realistic configuration.

Impact: Bounded behavior under pathological input. No observable difference from the reference for any configuration that the reference itself handles without stack-overflowing. Unit tests in `crates/irserve-core/src/rewrites.rs` exercise the cap with a synthetic 65-rule self-matching configuration and confirm the graceful clamp returns `Some("/a")` rather than panicking or 5xx-ing.

## D-015: Stage 6f un-defers error pages, full SRV-SEC-001, and custom headers

Date: 2026-05-10
Affected requirements: SRV-FILE-003 (custom `<status>.html`), SRV-SEC-001 (full wire-level surface), SRV-SEC-002 (single-pass URL decode), SRV-HDR-001 (custom headers — accumulate + override), SRV-HDR-002 (`value: null` deletes prior header).
Status: applied
Reason: Stage 6f generalizes phase 13 (error response), tightens phases 1 + 10 (decode + lexical containment), and adds a post-dispatch custom-header pass — closing the L2 cross-cutting bundle that the strict-L0 cutoff (D-008) and the deferred-decode notes in D-010/D-011 had left open. Cite `serve-handler/src/index.js:467-524` (`sendError`), `:561-580` (decode + containment), `:194-254` (`getHeaders`).

Methodological signals captured along the way:

1. **`traversal-raw-encoded.json` snapshot resync.** Stage 5b's L0-hygiene 404 collapsed `EscapedRoot` and `NotFound` into the same 404 response; Stage 6f flips path-traversal escapes to 400 (matching the reference). All four anchors in `traversal-raw-encoded.json` (`raw_dotdot_literal`, `raw_dotdot_percent_encoded`, `raw_double_slash`, `raw_malformed_percent`) graduated into `runner.l0.clean` with `bodyMayDiffer` (irserve emits the generic `<h1>STATUS REASON</h1>` body per D-003; reference emits the full `errorTemplate` HTML). The flip is delivery of the deferred contract, not a regression.

2. **3xx redirects skip header application.** `apply_custom_headers` short-circuits on `response.status().is_redirection()`. The reference's redirect path at `index.js:586-588` builds the response via `response.writeHead(redirect.statusCode, { Location: ... })` with no `getHeaders` call, so custom headers never layer onto 301/302/etc. Pinned by `headers-custom#html_get` (cleanUrls 301 case): reference snapshot emits only `location:`. Mirroring this avoids a snapshot divergence.

3. **`error_response` applies headers per-branch.** Codex review round 1 P1 surfaced that the prior implementation (a single top-level `apply_custom_headers` call after `dispatch_inner`) ignored reference's per-branch behavior. The corrective wiring inside `error_response`: JSON-preferring clients return BEFORE `getHeaders` (no custom headers — `index.js:477-487`); custom `<status>.html` branch matches headers against `/<status>.html` (`getHeaders(.., errorPage, stats)` at `index.js:508`); fallback HTML branch matches against the request path (`getHeaders(.., absolutePath, null)` at `index.js:519`). `dispatch_inner` returns `(Response, Option<String>)`; the outer `dispatch` wrapper applies headers only when `Some(path)` (success / 405 paths).

4. **`HeaderItem::value: null` is unreachable through the reference CLI.** `serve` validates `serve.json` against `@zeit/schemas/deployment/config-static.js`, which declares `value: { type: 'string', minLength: 1, maxLength: 2048, pattern: ... }`. The CLI refuses to start with a `value: null` entry, so SRV-HDR-002's contract is unreachable through the reference probe surface. IrServe accepts `value: null` (config.rs widens `HeaderItem::value` to `Option<String>`) and verifies the prune logic via the unit tests in `crates/irserve-core/src/custom_headers::tests` rather than an oracle probe.

5. **Null-prune is single-pass last-write-wins.** Codex review round 1 P1 surfaced that the prior two-pass implementation (insert non-null first, then walk again to delete any null entry) violated reference parity: an early-firing rule with `value: null` should NOT delete a value set by a later-firing rule with the same key. Reference's two-stage `Object.assign(defaultHeaders, related)` followed by the prune of FINAL `=== null` entries (`index.js:245-251`) is equivalent to a single-pass insert/remove walk over rules in declaration order. IrServe's `apply_custom_headers` now matches this; regression test `later_set_value_wins_over_earlier_null_prune` pins the corner.

6. **Strict UTF-8 in `try_percent_decode`.** Codex review round 1 P2 surfaced that `decode_utf8_lossy` admits requests like `/%FF` (valid `%xx` syntax but the byte 0xFF does not start a valid UTF-8 sequence) where reference's `decodeURIComponent` throws URIError → 400. Switched to strict `decode_utf8()` plus `Err`-on-invalid-UTF8 short-circuit. Closes D-010's deferred-from-6b promotion.

7. **Headers source is minimatch-only.** Codex review round 1 P1 surfaced that `Matcher::compile` routes `:name` sources through path-to-regexp (Pattern variant), which captures-and-matches `/api/42` for source `/api/:id`. Reference's `sourceMatches` for headers (the `getHeaders` call site at `index.js:207`) is invoked WITHOUT the truthy `allowSegments` argument, so path-to-regexp is skipped and the source is matched purely via minimatch — `:` is a literal char. Round 1 added `Matcher::compile_no_segments` for this; Codex review round 2 P1 then surfaced that the no-segments routing still landed on `Matcher::Literal::try_match`, whose first comparison is case-INSENSITIVE (path-to-regexp's `i` flag default), so source `/Case` wrongly matched `/case`. Round 2 superseded the `Matcher::compile_no_segments` helper with a slim `HeaderMatcher` enum (Literal / Glob) inside `custom_headers.rs`, bypassing `path_pattern::Matcher` entirely; Literal uses pure case-sensitive equality (mirrors minimatch's default `nocase: false`).

Impact: SRV-FILE-003, SRV-SEC-001 full, SRV-SEC-002, SRV-HDR-001, SRV-HDR-002 ship in Stage 6f. `notfound-shape.json#missing_html` keeps its `bodyMayDiffer` partition (D-003). 11 probes touched: `error-page-custom`, `notfound-custom`, `traversal-raw-encoded`, `traversal-encoded`, `headers-applied`, `headers-custom`, `headers-on-error` (NEW), `headers-accumulate` (NEW), `headers-after-cleanurls` (NEW round 2), `error-page-400-headers` (NEW round 3). New ORC IDs: ORC-159, ORC-160, ORC-161, ORC-162. After Codex rounds 1-3: 12 unit tests in `custom_headers::tests` cover the accumulate / case-insensitive / 3xx-skip / null-prune / `:name`-literal / null-prune-loses-to-later-set-value / case-sensitive-literal behaviors.

Codex review round 3 sharpened three corners of the per-branch error-response wiring:

8. **Custom `<status>.html` branch always applies headers.** Round 2 over-corrected by passing `&[]` to `error_response` from all 400 sites, which suppressed headers on the custom-page branch as well as the fallback HTML branch. Round 3 splits the controls: `error_response` always receives the real `header_rules` slice and an extra `skip_fallback_headers: bool`. The custom-page branch unconditionally applies headers matched against `/<status>.html` (mirrors `index.js:508`). The fallback HTML branch honors `skip_fallback_headers`: callers whose reference equivalent invokes `getHeaders` with an outside-root `absolutePath` (lexical-escape, malformed-decode, symlink-escape) pass `true`, and the fallback skips. ORC-162 pins that a `400.html` + `**` rule yields `x-error-page: yes` even when the 400 was triggered by `/../package.json`.

9. **Fallback HTML force-overrides Content-Type.** Reference forces `headers['Content-Type'] = 'text/html; charset=utf-8'` AFTER `getHeaders` in the fallback branch (`index.js:519-520`), so a user rule cannot make a 4xx fallback emit `application/x-custom`. IrServe now mirrors via a final `response.headers_mut().insert(CONTENT_TYPE, "text/html; charset=utf-8")` after `apply_custom_headers` runs in the fallback path. The custom-page branch keeps the merge-with-override semantics (a user Content-Type wins over the default `text/html`, matching reference's `Object.assign(defaultHeaders, related)`).

10. **Success-path header matching uses the lexical resolved URL, not the canonical PathBuf.** Round 2 derived the matching path from `ResolveOutcome::File(canonical_p).strip_prefix(canonical_root)` — but on Windows `tokio::fs::canonicalize` folds the file's case to its on-disk casing, so a request `GET /ASSET.CSS` resolved to a file named `asset.css` would match a `**/*.css` rule under irserve while reference's case-sensitive minimatch (working on the lexical `path.relative` of the un-canonicalized `absolutePath`) does not. Round 3 tracks a `lexical_url: String` alongside `ResolveOutcome` in `dispatch_inner`: the cleanUrls candidate it picked (`/page/index.html` or `/page.html`), the rewrite's destination string when a rewrite matched, otherwise the original `url_path`. The canonical `PathBuf` is used only for `tokio::fs::read`, never for header matching.

## D-016: `--no-port-switching` honors the documented contract (upstream no-op)

Date: 2026-05-10
Affected requirements: SRV-CLI-016
Status: adapted
Reason: The reference declares `--no-port-switching` at `third_party/serve/source/utilities/cli.ts:158` (clap-style flag declaration in `args` definition), but `server.ts`'s `startServer` never reads it: the busy-port branch at `server.ts:166-181` unconditionally retries on `port: 0` regardless of any flag value. The behavior is a known upstream regression — [vercel/serve#751](https://github.com/vercel/serve/issues/751), open since 2022-12, introduced in v14.0.0 (it worked in v13.0.4). On the reference, both default and `--no-port-switching` collapse to "retry on `port: 0`"; the documented contract ("refuse fallback") has been wire-unreachable for ~3 years. D-004 (no bug-for-bug parity) explicitly authorizes irserve to honor the **documented** contract rather than mirror the regression. We do not file a new upstream issue — duplicate. The probe runner already injects `--no-port-switching` for every reference invocation (`tools/probe/run.mjs:172`) but it is a no-op on the reference; the documented contract was never wire-tested upstream, which is precisely why the runner's reference behavior matches what the runner expected.
Impact: irserve's behavior:
- **default** (`--no-port-switching` absent): retry on `SocketAddr::new(addr.ip(), 0)` when the requested address returns `ErrorKind::AddrInUse`. Stderr emits a one-line warning (format implementation-defined per D-002).
- **`--no-port-switching` set**: surface `Error::PortInUse { addr }` for non-zero exit. Stderr emits a one-line error (format implementation-defined per D-002).

The bind/exit decision is contractual — D-002 was amended in the same change to clarify that only the warning/error wording is implementation-defined. Failure-mode is verified by integration tests in `crates/irserve-core/src/server.rs::tests` (`bind_fails_on_addr_in_use_when_switching_disabled`, `bind_retries_on_addr_in_use_when_switching_allowed`, `bind_succeeds_on_free_port`), not by an oracle probe — pre-binding a port from the runner before the server spawns is impractical for one case (would require a separate worker holding the socket across the spawn). The runner does NOT inject `--no-port-switching` for irserve invocations: irserve's default retry is unobservable on the runner's auto-allocated free ports, so injection would be cosmetic. Anyone validating irserve against the reference must remember the flag is a no-op upstream; comparison must use a test harness that actually pre-binds the requested port.

## D-017: ETag hash function: mirror reference (sha1 of extname + '-' + file contents)

Date: 2026-05-11
Affected requirements: SRV-CACHE-001.
Status: adapted
Reason: SRV-CACHE-001 ("ETag is sent by default and supports 304") fixes the **wire contract** — a strong `ETag` header on 200 file responses, plus a 304 short-circuit when `If-None-Match` matches verbatim — but is intentionally agnostic about the hash function. The reference computes ETag as `sha1(path.extname(absolutePath) + '-' + buf)` over the file's contents (`serve-handler/src/index.js:24-36`), strong-quoted as `"<40 hex chars>"` (`index.js:233`), and stashes the value on `stats.etag` for the 304 comparison (`index.js:760`). IrServe mirrors the formula byte-for-byte in `crates/irserve-core/src/etag.rs::compute_etag`: `sha1(extname.as_bytes() ++ b"-" ++ bytes)`, formatted `"\"<lower-hex>\""`. The choice of `sha1` and the `extname + '-' + bytes` framing is recorded here so future maintenance does not re-litigate the hash on grounds of "sha1 is cryptographically weak": this is a cache validator, not a security primitive. The user discussion in `docs/features/0015_PLAN_stage7a_etag_and_conditional_get.md` (Decision D1) confirmed the mirror choice on three grounds — (a) capture-replay in the probe runner (slice 4) neutralizes the value-divergence argument that would otherwise push toward a fresh hash, (b) mirroring keeps the spec one sentence and the contract surface minimum, (c) sha1 is fine for non-security cache-validator use. The same discussion pinned the conjunction predicate `req.headers.if-none-match === stats.etag && !req.headers.range` (`index.js:760`) as the irserve 304 trigger; the precedence on Range is a 7c precursor that lands here so 7c does not have to revisit `build_file_or_304`.
Impact: irserve's wire behavior:
- **ETag value** on 200 responses: `"<sha1(extname+'-'+bytes), lower-hex, 40 chars>"`. Byte-identical to the reference for the same file. Unit-tested against the `etag-roundtrip` fixture's pinned reference snapshot (`crates/irserve-core/src/etag.rs::tests`).
- **What is contractual**: the round-trip (200 emits ETag; matching `If-None-Match` short-circuits to 304 with no body / no `Content-Type` / no `ETag` echo). Verified by ORC-042 / ORC-043 under both `target=reference` and `target=irserve` via the probe runner's `$fromResponse` capture-replay.
- **What is NOT contractual**: the exact hex value. A future change to D-017 (e.g. switching to blake3, or to a content-only hash without the extname prefix) is permissible under D-004 (no bug-for-bug parity) provided the round-trip survives. The probe is hash-agnostic by construction.
- **Forward note**: the reference maintains an in-memory cache `Map<absPath, [mtime, sha]>` at `serve-handler/src/index.js:22` consulted at `:228-231` to avoid hashing on every request. IrServe hashes on every request. This is a future performance optimization, not contractual; if it lands later (Stage 7+ or as a separate perf change), the cache key (mtime vs content hash vs file inode) is implementation-defined and SHOULD be documented as a follow-up D-NNN if it implies any observable behavior (e.g. an stale-cache invalidation policy).
