{
  "id": "etag-roundtrip",
  "description": "ETag round-trip: request a non-html file (no cleanUrls redirect), capture its ETag, then re-request with matching If-None-Match. Expect 304 and an empty body. Second request's If-None-Match is resolved via `$fromResponse` from `first_get` so the case stays hash-agnostic across reference and irserve targets.",
  "fixture": {
    "files": {
      "asset.css": "body{color:red}\n"
    }
  },
  "requests": [
    { "name": "first_get", "method": "GET", "path": "/asset.css" },
    {
      "name": "second_with_inm",
      "method": "GET",
      "path": "/asset.css",
      "headers": {
        "if-none-match": { "$fromResponse": { "request": "first_get", "header": "etag" } }
      }
    }
  ],
  "runner": {
    "l0": {
      "clean": ["first_get", "second_with_inm"]
    }
  }
}
