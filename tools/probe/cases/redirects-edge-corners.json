{
  "id": "redirects-edge-corners",
  "description": "Codex round 10 corners: (P1.1) `\\**` parses as globstar in minimatch (de-escape before `**` check); (P1.2) per-alt dot rule for braces — `{.x,*}` admits `.x` via dot alt and `abc` via `*` alt but REJECTS `.y` because the `*` alt obeys `dot: false`; (P2) `\\[` falls back to a Literal match against `[` (the de-escaped pattern is invalid globset, so the classifier degrades gracefully).",
  "fixture": {
    "files": {
      "_marker.txt": "marker\n"
    },
    "serveJson": {
      "redirects": [
        { "source": "/a/\\**", "destination": "/escaped-doublestar-hit" },
        { "source": "/x/{.x,*}", "destination": "/brace-mix-hit" },
        { "source": "/u/\\[", "destination": "/lbracket-hit" }
      ]
    }
  },
  "runner": {
    "l0": {
      "clean": [
        "escaped_doublestar_matches_multi",
        "brace_mix_matches_dot_alt",
        "brace_mix_matches_star_alt",
        "brace_mix_rejects_other_dotfile",
        "literal_lbracket_via_fallback"
      ],
      "contentLengthMayDiffer": [
        "escaped_doublestar_matches_multi",
        "brace_mix_matches_dot_alt",
        "brace_mix_matches_star_alt",
        "brace_mix_rejects_other_dotfile",
        "literal_lbracket_via_fallback"
      ],
      "bodyMayDiffer": ["brace_mix_rejects_other_dotfile"]
    }
  },
  "requests": [
    { "name": "escaped_doublestar_matches_multi", "method": "GET", "path": "/a/x/y" },
    { "name": "brace_mix_matches_dot_alt", "method": "GET", "path": "/x/.x" },
    { "name": "brace_mix_matches_star_alt", "method": "GET", "path": "/x/abc" },
    { "name": "brace_mix_rejects_other_dotfile", "method": "GET", "path": "/x/.y" },
    { "name": "literal_lbracket_via_fallback", "method": "GET", "path": "/u/[" }
  ]
}
